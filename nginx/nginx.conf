# user  nginx;
# worker_processes  1;
# events {
#     worker_connections  1024;
# }
# http {
#     include       mime.types;
#     sendfile       on;
#     include /etc/nginx/conf.d/*.conf;
# }

worker_processes  1;
events {
    worker_connections  1024;
}
http {
    include       mime.types;
    sendfile       on;
    include /etc/nginx/conf.d/*.conf;
}
